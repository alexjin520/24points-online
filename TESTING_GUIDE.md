# 单人练习模式测试指南

## 启动应用程序

### 1. 启动服务器（需要两个终端窗口）

**终端1 - 启动后端服务器:**
```bash
cd 24points-online/server
npm run dev
```

**终端2 - 启动前端客户端:**
```bash
cd 24points-online/client  
npm run dev
```

### 2. 访问应用程序
- 打开浏览器访问: `http://localhost:5173`
- 服务器运行在: `http://localhost:3024`

## 测试单人练习模式

### 1. 进入练习模式
1. 打开应用程序主页
2. 在大厅界面，找到紫色渐变的"🎯 单人练习模式"按钮
3. 点击按钮进入练习模式

### 2. 基本功能测试

#### 题目生成测试
- 验证页面显示4张卡片
- 每次点击"下一题"应该生成新的卡片组合
- 所有生成的题目都应该是可解的

#### 答案输入测试
```
测试用例1: 正确答案
输入: (6+6+6)*4  (如果卡片是 6,6,6,4)
预期: 显示 "🎉 正确!" 

测试用例2: 错误答案  
输入: 6+6+6+4
预期: 显示 "❌ 错误！结果是22，不是24"

测试用例3: 格式错误
输入: 6++6
预期: 显示 "❌ 表达式格式错误"

测试用例4: 数字不匹配
输入: 1+2+3+4  (如果卡片不是这些数字)
预期: 显示错误信息
```

#### 统计功能测试
- 答对题目后，"已答题"数量应该增加
- 答对题目后，"连胜"应该增加
- 答错题目后，"连胜"应该重置为0
- "正确率"应该正确计算百分比

#### 提示功能测试
- 点击"获取提示"按钮
- 应该显示 "💡 提示：这道题确实有解，试试不同的运算符组合！"
- 点击后按钮应该变为禁用状态："已显示提示"

### 3. 用户体验测试

#### 键盘操作
- 在输入框中输入表达式后按Enter键
- 应该自动提交答案

#### 视觉反馈
- 答对后输入框应该显示绿色边框
- 答错后输入框应该显示红色边框

#### 响应式设计
- 在不同屏幕尺寸下测试界面布局
- 移动设备上的显示效果

### 4. 导航测试
- 点击"← 返回大厅"按钮应该回到主大厅
- 可以重新进入练习模式

## 常见表达式示例

```
如果卡片是 [6, 6, 6, 6]:
- (6+6+6)*6 = 24 ✓
- 6*6-6-6 = 24 ✓

如果卡片是 [3, 8, 8, 3]:
- (8-3)*3+8 = 23 ❌ 
- 8*3+8-3 = 29 ❌
- 8/(3/3)*8 = 64 ❌
- (8+8)*(3-3) = 0 ❌
```

## 错误排查

### 如果练习模式按钮不出现
1. 检查浏览器控制台是否有JavaScript错误
2. 确认客户端服务器正常启动
3. 刷新页面重试

### 如果卡片不显示
1. 检查网络连接
2. 查看浏览器控制台错误信息
3. 确认TypeScript编译无错误

### 如果答案验证不工作
1. 确保输入的数字与显示的卡片完全匹配
2. 检查表达式语法是否正确
3. 尝试使用括号明确运算顺序

## 性能测试

1. **题目生成速度**: 点击"下一题"应该立即生成新题目
2. **答案验证速度**: 提交答案后应该立即显示结果
3. **内存使用**: 长时间使用不应该导致内存泄漏

## 兼容性测试

### 浏览器支持
- Chrome 90+
- Firefox 88+  
- Safari 14+
- Edge 90+

### 设备支持
- 桌面电脑
- 平板设备
- 智能手机

## 反馈和改进建议

测试过程中如果发现任何问题，请记录：
1. 操作步骤
2. 预期结果
3. 实际结果
4. 浏览器和设备信息
5. 错误信息截图

这将帮助快速定位和修复问题。 